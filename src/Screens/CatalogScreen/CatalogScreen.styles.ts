import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  columnWrapper: {
    columnGap: 4,
  },
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  listContainer: {
    gap: 4,
  },
  text: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 12,
  },
});
